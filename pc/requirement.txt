# Зависимости стендовых инструментов в pc/tools/.
#
# Обязательное: обмен с платой по serial.
pyserial>=3.5

# Графики инструментов настройки и живой панели состояния.
# Настройка возможна и без них: инструменты печатают метрики в терминал
# и рисуют однострочный график, а окна просто не открываются.
#
# PyQtGraph выбран вместо matplotlib из-за частоты обновления: при настройке
# график перерисовывается десятки раз в секунду.
pyqtgraph>=0.13
PyQt5>=5.15
numpy>=1.24

# Устаревшее: остался только в teleop_keyboard.py.
matplotlib>=3.8
