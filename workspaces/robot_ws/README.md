# Real-robot workspace

Compose mounts the hardware, localization, Nav2, and city-navigation package
sources into `src/`. Colcon writes
`build/`, `install/`, and `log/` here so they survive container recreation.
