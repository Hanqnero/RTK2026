# Wheel velocity command from Pi to Arduino
# Units: ticks/sec (signed), converted from cmd_vel by base_controller

int16 left_tps    # target velocity, left wheel (ticks/sec)
int16 right_tps   # target velocity, right wheel (ticks/sec)
