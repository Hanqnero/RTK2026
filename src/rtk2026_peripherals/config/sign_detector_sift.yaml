detect_sign_sift:
  ros__parameters:
    dataset_root: /workspace/yolo/dataset
    camera_topic: /camera/color/image_raw
    ratio_thresh: 0.75
    min_good: 6
    min_inliers: 6
    min_inlier_ratio: 0.30
    max_refs_per_class: 3
    every_n: 2

